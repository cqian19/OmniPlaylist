/**
 * Created by cqian19 on 5/21/2017.
 */

const defaultState = {};

export function navbarReducer(state = defaultState, action) {
    switch (action.type) {
        default :
            return state;
    }
}